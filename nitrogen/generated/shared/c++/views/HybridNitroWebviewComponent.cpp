///
/// HybridNitroWebviewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridNitroWebviewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitrowebview::views {

  extern const char HybridNitroWebviewComponentName[] = "NitroWebview";

  HybridNitroWebviewProps::HybridNitroWebviewProps(const react::PropsParserContext& context,
                                                   const HybridNitroWebviewProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    sourceUrl([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("sourceUrl", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.sourceUrl;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.sourceUrl);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroWebview.sourceUrl: ") + exc.what());
      }
    }()),
    onScriptLoaded([&]() -> CachedProp<std::optional<std::function<void()>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onScriptLoaded", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onScriptLoaded;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void()>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onScriptLoaded);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroWebview.onScriptLoaded: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nitrowebview::HybridNitroWebviewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::nitrowebview::HybridNitroWebviewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroWebview.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridNitroWebviewProps::HybridNitroWebviewProps(const HybridNitroWebviewProps& other):
    react::ViewProps(),
    sourceUrl(other.sourceUrl),
    onScriptLoaded(other.onScriptLoaded),
    hybridRef(other.hybridRef) { }

  bool HybridNitroWebviewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("sourceUrl"): return true;
      case hashString("onScriptLoaded"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroWebviewComponentDescriptor::HybridNitroWebviewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  react::Props::Shared HybridNitroWebviewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                         const react::Props::Shared& props,
                                                                         react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroWebviewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroWebviewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridNitroWebviewShadowNode&>(shadowNode);
    const HybridNitroWebviewProps& props = concreteShadowNode.getConcreteProps();
    HybridNitroWebviewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitrowebview::views
